import React from "react";

import HeroText from "./HeroText";

const Hero = () => {
  return (
    <section className="bg-sky-600 text-white py-40">
      <HeroText />
    </section>
  );
};

export default Hero;
